import { IEndpointsConfig } from '../../models/common/endpoints.config';
import { EdgeSDKConfig } from '../edgeSDK';
export declare class ConfigProvider {
    private readonly INTERNAL_EDGE_API_HOST;
    private readonly INTERNAL_EDGE_MQTT_HOST;
    private readonly INTERNAL_EDGE_IOT_HOST;
    private readonly INTERNAL_EDGE_PORT;
    private readonly INTERNAL_CONFIG_PATH;
    private readonly config;
    getEndpoints(config: EdgeSDKConfig): IEndpointsConfig;
    private initEndpoints;
    private readConfigFile;
}
//# sourceMappingURL=config.provider.d.ts.map