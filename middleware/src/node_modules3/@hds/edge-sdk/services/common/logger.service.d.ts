import { LOG_LEVEL, ILoggingConfiguration, ILoggingConfigurationSerializable } from '../../models/common/logger.model';
export interface ILogService {
    log(level: LOG_LEVEL, message: string): void;
    getConfig(): ILoggingConfiguration;
    updateConfig(logConfig: ILoggingConfigurationSerializable): ILoggingConfiguration;
}
export declare class LogService implements ILogService {
    static initialize(configFilePath: string): ILogService;
    static log(level: LOG_LEVEL, message: string): void;
    private static _instance;
    private config;
    private logger;
    private configFilePath;
    constructor(configFilePath: string);
    log(level: LOG_LEVEL, message: string): void;
    getConfig(): ILoggingConfiguration;
    updateConfig(logConfig: ILoggingConfigurationSerializable): ILoggingConfiguration;
    private updateConfigValues;
    private initDefaultConfiguration;
    private loadConfig;
    private saveConfig;
    private readConfigFile;
    private writeConfigFile;
}
//# sourceMappingURL=logger.service.d.ts.map