/**
 * @module Historian
 */
import { IEdgeDataMessageInfo, IEdgeData, EdgeData } from "../../edgeData";
/**
 * The interface that is used by the Historian to pass data to edge clients.
 * Contains a list of variables that represents the data columns.
 * Data can come as a single or multi row object of any type supported by JSON
 */
export interface IEdgeHistorianData extends IEdgeData {
    /** Message metadata */
    info: IEdgeHistorianMessageInfo;
    /** Variables representing data columns  */
    variables: string[];
    /** Row(s) of data organized by the variables array */
    data: any[][];
}
/**
 * Metadata representing the contents of an [[IEdgeHistorianMessage]]
 */
export interface IEdgeHistorianMessageInfo extends IEdgeDataMessageInfo {
    /** The Well ID of the data */
    well: string;
    /** The Run ID of the data */
    run: string;
    /**
     * The record name of the data
     */
    record: string;
    /**
     * Is the record from the WellBased table or from a run
     */
    isWellBased: boolean;
    /**
     * The descriptor the data came from under the record
     */
    descriptor: string;
    /**
     * number of rows expected in the dataset
     */
    numRows: number;
}
/**
 * An implementation of [[IEdgeHistorianData]] which gives access to helper functions
 * from its base class [[EdgeData]] that are useful for parsing its data.
 */
export declare class EdgeHistorianData extends EdgeData implements IEdgeHistorianData {
    info: IEdgeHistorianMessageInfo;
}
//# sourceMappingURL=edgeHistorianData.d.ts.map