/**
 * Config to setup all needed RMQ channels
 */
export interface IRabbitMqConfig {
    subscriberConfig?: IRabbitMQSetting[];
    publisherConfig?: IRabbitMQSetting;
}
/**
 * Config to setup RMQ channel
 */
export interface IRabbitMQSetting {
    type: string;
    exchange: string;
    exchangeType: string;
    queueExclusive: boolean;
    routingKey: string;
    durable: boolean;
}
//# sourceMappingURL=rabbit-mq.config.model.d.ts.map