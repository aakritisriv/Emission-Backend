import { IRabbitMqConfig } from '../common/rabbit-mq.config.model';
export interface ICommonMiddlewareConfig {
    /**
     * Provide this config to enable common middleware RMQ auto subscription
     * and publish back functionality
     */
    rabbitMqConfig?: IRabbitMqConfig;
    useDbCaching?: boolean;
    webSocketPublish?: boolean;
}
export declare class CommonMiddlewareConfig implements ICommonMiddlewareConfig {
    rabbitMqConfig?: IRabbitMqConfig;
    useDbCaching?: boolean;
    webSocketPublish?: boolean;
}
//# sourceMappingURL=common-middleware.config.d.ts.map