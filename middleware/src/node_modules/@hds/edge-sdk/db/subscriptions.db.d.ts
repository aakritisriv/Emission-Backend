import { IDataSubscription } from '../models/middleware/data-subscription.model';
import { IDatabaseConnection } from './databaseConnection';
export interface ISubscriptionsDB {
    cacheSubscription(subscription: IDataSubscription): Promise<void>;
    getCachedSubscriptions(): Promise<IDataSubscription[]>;
    removeSubscription(uniqueName: string): Promise<void>;
}
export declare class SubscriptionsDB implements ISubscriptionsDB {
    private db;
    private subscriptionModel;
    private readonly _subscriptionSchema;
    constructor(db: IDatabaseConnection);
    cacheSubscription(subscription: IDataSubscription): Promise<void>;
    removeSubscription(uniqueName: string): Promise<void>;
    getCachedSubscriptions(): Promise<IDataSubscription[]>;
}
//# sourceMappingURL=subscriptions.db.d.ts.map