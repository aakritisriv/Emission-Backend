import { IResultStatusObject, ResultStatus } from '../models/api/resultStatusObject';
import mongoose = require('mongoose');
export interface IDatabaseConnection {
    mongoose: typeof mongoose;
    setErrorResult<T>(errorString: string, result: IResultStatusObject<T>, status?: ResultStatus): void;
}
export declare class MongoDbConnection implements IDatabaseConnection {
    private connection;
    get mongoose(): typeof mongoose;
    constructor(connection: string);
    setErrorResult<T>(errorString: string, result: IResultStatusObject<T>, status?: ResultStatus): void;
    private initializeConnection;
    private initialize;
}
//# sourceMappingURL=databaseConnection.d.ts.map