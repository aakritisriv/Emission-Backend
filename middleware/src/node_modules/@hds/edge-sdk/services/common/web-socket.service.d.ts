/// <reference types="node" />
import { Server } from 'http';
export interface IWebSocketService {
    broadcastMessageToDefault(msg: any, messageName: any): void;
    broadcastToNamespace(namespace: string, messageName: string, messageData: any): void;
    addNamespace(name: string): void;
    removeNamespace(name: string): void;
}
export declare class EdgeWebSocketService {
    private server;
    private socket;
    private port;
    constructor(server: Server);
    broadcastMessageToDefault(msg: any, messageName: any): void;
    broadcastToNamespace(namespace: string, messageName: string, messageData: any): void;
    addNamespace(name: string): void;
    removeNamespace(name: string): void;
    private listen;
}
//# sourceMappingURL=web-socket.service.d.ts.map