import { IDataProcessor } from '../../services/middleware/data-processor.service';
import { IDataSubscriptionManager } from '../../services/middleware/data-subscription-manager';
import { BaseRoute } from '../route';
/**
 * Express router to enable data subscription API routes
 * data subscriptions used for UIs to subscribe for specific variables data retrieval
 */
export declare class CommonMiddlewareDataSubscriptionRouter extends BaseRoute {
    readonly path = "/api/v1/subscription";
    private dataProcessor;
    private dataSubscription;
    constructor(dataProcessor: IDataProcessor, dataSubscription: IDataSubscriptionManager);
    /**
     * GET /all - returns all registered subscriptions info
     */
    getRegisteredSubscriptions: (req: any, res: any) => Promise<void>;
    /**
     * POST /register - registers data subscription under uniqueName key
     */
    registerSubscription: (req: any, res: any) => Promise<void>;
}
//# sourceMappingURL=common-middleware-data-subscription.router.d.ts.map