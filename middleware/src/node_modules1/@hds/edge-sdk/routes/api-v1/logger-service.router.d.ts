import { ILogService } from '../../services/common/logger.service';
import { BaseRoute } from '../route';
import { IServiceRouter } from '../service-router.interface';
/**
 * Express Router to enable logger configuration by REST API
 */
export declare class LoggerServiceRouter extends BaseRoute implements IServiceRouter {
    readonly path = "/api/v1/logger";
    private logService;
    constructor(logService: ILogService);
    /**
     * GET /levels - logging levels
     */
    getLoggingLevels: (req: any, res: any, next: any) => Promise<void>;
    /**
     * GET /config - logging configuration
     */
    getConfig: (req: any, res: any, next: any) => Promise<void>;
    /**
     * POST /config - logging configuration
     */
    setConfig: (req: any, res: any, next: any) => Promise<void>;
    /**
     * Take each handler, and attach to one of the Express.Router's
     * endpoints.
     */
    private init;
}
//# sourceMappingURL=logger-service.router.d.ts.map